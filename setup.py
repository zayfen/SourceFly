from setuptools import setup

package = "sourcefly"
version = "0.1"

setup(
    name=package,
    version=version,
    description="extract all source files by specifing a entry file",
    url="https://sourcefly.zayfen.com",
)
